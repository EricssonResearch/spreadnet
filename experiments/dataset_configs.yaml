data:
  # graph generator
  random_seed: 0
  num_node_min: 8
  num_node_max: 50
  starting_theta: 15 # the higher the less edge
  min_path_length: 1
  visualize_graph: 25 # less than or equal to dataset_size

  # dataset generator
  dataset_path: "dataset"
  dataset_size: 5120
